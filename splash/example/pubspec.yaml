name: example
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.0
  flutter: '>=2.0.0'

dependencies:
  flutter:
    sdk: flutter
  splash:
    path: ../../splash

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true